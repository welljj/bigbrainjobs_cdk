{
  "name": "bigbrainjobs",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "source": "static/src/main.js",
  "targets": {
    "default": {
      "distDir": "static/dist"
    }
  },
  "scripts": {
    "watch": "parcel watch",
    "build": "parcel build --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "latest"
  },
  "dependencies": {
    "@popperjs/core": "latest",
    "bootstrap": "latest"
  }
}